# Assets

This task does not rely on a static external image bank.

- Word-pair stimuli are generated deterministically from the local cue/associate banks in `src/utils.py`.
- All participant-facing screens are text-based and are configured in `config/*.yaml`.
- The generated figure preview lives at `task_flow.png` in the task root.
