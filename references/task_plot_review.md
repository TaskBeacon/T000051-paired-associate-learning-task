# Task Plot Review

- Flow: Diagram shows study trials followed by test trials for practice, scored list 1, and scored list 2.
- Conditions: Diagram covers `practice`, `scored_1`, and `scored_2`, with related/unrelated study pairs and 4-choice test recognition.
- Timing: Diagram uses 500 ms fixation, 1500 ms study, 400 ms study ITI, 6000 ms test, 400 ms test ITI, and 800 ms practice feedback.
- Keys: Study keys are `R`/`U`; test keys are `1`/`2`/`3`/`4`.
- README embed: `README.md` contains `![Task Flow](task_flow.png)` under `## 2. Task Flow`.
- Logo/header: Raw model output leaves blank header space; final `task_flow.png` is produced by `apply_taskbeacon_watermark.ps1`.
